<div class="jumbotron">
    <h1 class="display-4">Bem-vindo ao {{title}}!</h1>
    <p class="lead">Sistema de gerenciamento de adoção de pets.</p>
    <hr class="my-4">
    <p>Escolha uma das opções abaixo para começar:</p>
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
        {{#each features}}
        <div class="col">
            <div class="card h-100">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title">{{this.name}}</h5>
                    <p class="card-text flex-grow-1">{{this.description}}</p>
                    <a href="{{this.path}}" class="btn btn-primary mt-auto">Acessar</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>